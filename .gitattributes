# Shell scripts are installed on Linux servers; a CRLF checkout makes them
# unrunnable ("bad interpreter"), so keep them LF everywhere.
deploy/scripts/*.sh text eol=lf
*.service text eol=lf
*.conf text eol=lf
